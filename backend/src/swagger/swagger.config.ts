import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: '2FA API',
  description: '2FA API swagger end-point description',
  version: '1.0',
  tags: ['Template'],
};
